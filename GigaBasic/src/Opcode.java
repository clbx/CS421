
public enum Opcode { none, noop, mov, 
					add, sub, mul, div, 
				    cmp, test,
				    jmp, je, jne, jz, jnz, jl, jle, jg, jge,
				    sete, setne, setl, setle, setg, setge,
				    and, or, shl, shr,
				    push, pop,
				    call, ret, leave
				} 

